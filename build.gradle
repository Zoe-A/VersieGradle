apply plugin: 'java'

apply plugin: 'application'

mainClassName = "week2.FizzBuzz"
run.args = [" Zoe", "2", "dsfsdf"]

repositories{
    mavenCentral()
}

/*run { standardInput = System.in }*/ // om een scanner te laten werken

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

